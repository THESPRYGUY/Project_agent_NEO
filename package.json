{
  "name": "project-agent-neo",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=dot"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}

