name: kpi-gate
on:
  pull_request:
    branches: [ staging, main ]
jobs:
  kpi:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: KPI Runner (stub)
        run: ci/kpi_runner_stub.sh
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: kpi-artifacts
          path: artifacts/
